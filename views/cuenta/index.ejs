<h1>cuenta</h1>


<% if (result.length == 0) { %>
  <h4>
    0 cuenta
  </h4>
<% } else { %>
<table>
	<thead>
    <th>#</th>
		<th>id</th>
			<th>createdAt</th>
			<th>updatedAt</th>
			<th>id</th>
			<th>nombre</th>
			<th>codigo</th>
			<th>idOperacion</th>

		<th>created at</th>
		<th>updated at</th>
		<th colspan="3"></th>
	</thead>

	<tbody>
		<% result.forEach((item, i) => { %>
			<tr>
				<td>
					<%= (i + 1) %>
				</td>
				<td>
					<%= item.id %>
				</td>
					<td>
						<%= item.createdAt %>
					</td>
					<td>
						<%= item.updatedAt %>
					</td>
					<td>
						<%= item.id %>
					</td>
					<td>
						<%= item.nombre %>
					</td>
					<td>
						<%= item.codigo %>
					</td>
					<td>
						<%= item.idOperacion %>
					</td>
				<td>
					<%= item.createdAt %>
				</td>
				<td>
					<%= item.updatedAt %>
				</td>
				<td>
					<a href="/cuenta/show/<%= item.id %>">Show</a>
				</td>
				<td>
					<a href="/cuenta/delete/<%= item.id %>">Delete</a>
				</td>
        <td>
					<a href="/cuenta/edit/<%= item.id %>">Edit</a>
				</td>
			</tr>
			<% }) %>
	</tbody>
</table>

<% } %>

<br>
<a href="/cuenta/create">New</a>